/** Voice/tone passed to demo performance as `style_prompt` for the Orbit coach. */
export const ORBIT_PERFORMANCE_STYLE_PROMPT =
  'Address the operator directly in second person ("you", "your"). Use a concise, supportive product-coach tone. Never refer to them as "the learner", "they", or "their" when describing what to do next.';